# Terraform (optional)

This directory is reserved for AWS resources (S3, MSK, Glue, Snowflake networking) used in production deployments.

Local development uses Docker Compose (Kafka, MinIO, Postgres, Airflow) instead of applying Terraform by default.
